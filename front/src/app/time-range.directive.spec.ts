import { TimeRangeDirective } from './time-range.directive';

describe('TimeRangeDirective', () => {
  it('should create an instance', () => {
    const directive = new TimeRangeDirective();
    expect(directive).toBeTruthy();
  });
});
